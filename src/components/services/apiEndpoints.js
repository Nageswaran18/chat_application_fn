import config from "../config/config.json"

const apiEndpoints = {
    home : `/home/`,
    chat_message_get : '/get-message/',
    notification : '/notifications/',
    notification_patch: '/mark-as-read/',

}

export default apiEndpoints;
